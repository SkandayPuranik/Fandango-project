import numpy as np
import pandas as pd

df = pd.read_csv("c://Users//Admin//Documents//Coding//Data science//Pandas//movie_scores.csv")

# Options:
# Keep it
# Remove it
# Replace it

df[(df['pre_movie_score'].isnull()) & (df['first_name'].notnull())]
df.dropna(thresh=1)
df.dropna(subset=['last_name'])
df['pre_movie_score'].fillna(df['pre_movie_score'].mean())
