import numpy as np
import pandas as pd

df = pd.read_csv("c://Users//Admin//Documents//Coding//Data science//Pandas//mpg.csv")

print(df.groupby('model_year')(int).mean()['mpg'])
year_cyl = df.groupby(['model_year', 'cylinders']).mean()

year_cyl.xs(key=70, level='model_year')
